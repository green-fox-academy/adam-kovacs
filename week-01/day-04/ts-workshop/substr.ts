'use strict';
export { };

//  Create a function that takes two strings as a parameter
//  Returns the starting index where the second one is starting in the first one
//  Returns `-1` if the second string is not in the first one
function substr(str: string, keyword: string): number {
  let index: number;
  for (let i: number = 0; i <= str.length; i++) {
    
  }

  return;
}
//  Example
console.log(('this is what I\'m searching in').indexOf('searching'));
// should print: `17`
console.log(('this is what I\'m searching in').indexOf('not'));
// should print: `-1`
// The function is already created, it's indexOf

